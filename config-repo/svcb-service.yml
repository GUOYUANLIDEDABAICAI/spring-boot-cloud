server:
  port: 8070
  
msg: Hello

eureka:
  instance:
    metadata-map:
      user.name: user
      user.password: 123

security:
  user:
    name: user
    password: 123
  oauth2:
    client:
      clientId: svcb-service
      clientSecret: password
      accessTokenUri: http://localhost:8060/aaa/oauth/token
      grant-type: client_credentials
      scope: server
