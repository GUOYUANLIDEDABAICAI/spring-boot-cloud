server:
  port: 8080

name: zhangxd

eureka:
  instance:
    metadata-map:
      user.name: anil
      user.password: password

security:
  user:
    name: user
    password: 123
  oauth2:
    client:
      clientId: svca-service
      clientSecret: 123
      accessTokenUri: http://localhost:8060/aaa/oauth/token
      grant-type: client_credentials
      scope: server
    resource:
      userInfoUri: http://localhost:8060/current
